import { helloSectionContent, bigLogoForSection, isButtonConnect, button } from './content';

export const HelloSectionOption = {
  helloSectionContent,
  bigLogoForSection,
  isButtonConnect,
  button,
};
