import { ComplianceSectionType } from '../../../../../../app/Landing/types';
import { img, head, description, button, isArrowIconInButton } from './content';

export const ComplianceSectionOption: ComplianceSectionType = {
  img,
  head,
  description,
  button,
  isArrowIconInButton,
};
